# The path to search for git repositories in
root: ../

# Run the application in debug mode
debug: true

# The description of each git repository
descriptions:
  simple: A simple description
  multi-line: |
    A multiline description.
    This is the second line.
